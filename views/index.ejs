<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Chatbot</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #1e1e1e;
            color: #ffffff;
        }

        .container {
            background-color: #1f2629;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.2);
            text-align: center;
            width: 70%;
        }

        h1 {
            font-size: 30px;
            margin-bottom: 25px;
            color: #4c8297;
        }

        form {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        label {
            font-size: 18px;
            color: #4c8297;
        }

        input[type="text"] {
            padding: 15px;
            border: 2px solid #616161;
            background-color: #2a2f37;
            color: #ffffff;
            border-radius: 5px;
            font-size: 16px;
            outline: none;
            transition: border-color 0.3s;
        }

        input[type="text"]:focus {
            border-color: #4c8297;
        }

        button {
            padding: 15px;
            background-color: #4c8297;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #366b79;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Welcome to Product Chatbot</h1>
        <form id="product-form">
            <label for="product-name">Enter Product Name:</label>
            <input type="text" id="product-name" name="product-name" required>
            <button type="submit">Start</button>
        </form>
    </div>

    <script>
        document.getElementById('product-form').addEventListener('submit', (e) => {
            e.preventDefault();
            const productName = document.getElementById('product-name').value;

            // Redirect to the /questions page with productName as a query parameter
            window.location.href = `/questions?productName=${encodeURIComponent(productName)}`;
        });
    </script>
</body>

</html>
