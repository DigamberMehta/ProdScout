<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Recommendations</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(135deg, #e0f7fa, #80deea, #26c6da);
        }

        .container {
            background-color: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            text-align: center;
            width: 100%;
            max-width: 600px;
            height: fit-content;
            margin-top: 675px;
        }

        h2 {
            color: #006064;
            margin-bottom: 20px;
            font-size: 24px;
            font-weight: 600;
        }

        #recommendations-container {
            text-align: left;
            margin-bottom: 20px;
        }

        div {
            margin-bottom: 15px;
        }

        input[type="radio"] {
            margin-right: 10px;
        }

        label {
            font-size: 18px;
            color: #004d40;
        }

        button {
            padding: 12px 20px;
            background-color: #006064;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s;
            margin-top: 20px;
        }

        button:hover {
            background-color: #004d40;
        }
    </style>
</head>

<body>
    <div class="container">
        <h2>Product Recommendations:</h2>
        <form id="product-selection-form" action="/results" method="POST">
            <div id="recommendations-container">
                <!-- Server-side code would render recommendations here -->
                <% recommendations.forEach((recommendation, index) => { %>
                    <div class="form-check">
                        <input type="radio" class="form-check-input" id="product-<%= index %>" name="selectedProduct" value="<%= recommendation %>" required>
                        <label class="form-check-label" for="product-<%= index %>"><%= recommendation %></label>
                    </div>
                <% }) %>
            </div>
            <button type="submit" class="btn btn-primary">Submit Selected Product</button>
        </form>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>

</html>
